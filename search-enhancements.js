/* StrettoCharts search: structured entity search, current + historical lookup, and trusted artwork fallback. */
(function(){
'use strict';
const d=document,$=s=>d.querySelector(s);
const norm=s=>String(s??'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/&/g,' and ').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();
const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
const text=(e,...keys)=>{for(const k of keys)if(e?.[k]!=null&&String(e[k]).trim())return String(e[k]).trim();return ''};
const album=e=>text(e,'album','albumName','release','releaseTitle');
const artists=e=>Array.isArray(e?.artists)?e.artists:[];
const lev=(a,b)=>{if(a===b)return 0;if(!a)return b.length;if(!b)return a.length;let p=Array.from({length:b.length+1},(_,i)=>i);for(let i=1;i<=a.length;i++){const q=[i];for(let j=1;j<=b.length;j++)q[j]=Math.min(q[j-1]+1,p[j]+1,p[j-1]+(a[i-1]===b[j-1]?0:1));p=q}return p[b.length]};
let current=null,all=[],index=[],selected=null,timer=0,request=0;const artworkCache=new Map();
function style(){if($('#stretto-search-css'))return;const s=d.createElement('style');s.id='stretto-search-css';s.textContent=`
.search-suggestions{position:absolute;left:0;right:0;top:64px;z-index:100;background:#fff;color:#171722;border:1px solid #e4e4ed;border-radius:18px;padding:7px;box-shadow:0 18px 45px #0004;display:none;max-height:390px;overflow:auto}.search-suggestions.open{display:block}.suggestion{width:100%;display:grid;grid-template-columns:42px 1fr auto;gap:10px;align-items:center;text-align:left;border:0;background:transparent;border-radius:12px;padding:9px;cursor:pointer}.suggestion:hover{background:#f3f1fb}.suggestion-art,.candidate-art{object-fit:cover;background:linear-gradient(135deg,#ff3d81,#7657ff)}.suggestion-art{width:42px;height:42px;border-radius:10px}.suggestion-main{min-width:0}.suggestion-main strong,.suggestion-main span{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.suggestion-main span{font-size:10px;color:#717282;margin-top:3px}.suggestion-type{font-size:9px;font-weight:850;text-transform:uppercase;color:#6252aa}.search-status{margin-top:8px;color:#c9c5d7;font-size:12px;min-height:18px}.search-results-panel{border-radius:23px;padding:20px;background:#fff;border:1px solid #e4e4ed;box-shadow:0 8px 28px #24243b0d}.result-sections{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px;align-items:start}.result-section{min-width:0}.result-section.other{grid-column:1/-1}.result-section-head{display:flex;justify-content:space-between;gap:8px;align-items:baseline;margin-bottom:8px}.result-section-head strong{font-size:13px}.result-section-head span{font-size:10px;color:#717282}.candidate-grid{display:grid;gap:9px}.candidate{width:100%;min-height:80px;display:grid;grid-template-columns:58px 1fr;gap:12px;text-align:left;align-items:center;border:1px solid #e4e4ed;background:#fafafd;border-radius:15px;padding:10px;cursor:pointer}.candidate:hover,.candidate:focus-visible{border-color:#7657ff;outline:0}.candidate-art-wrap{width:58px;height:58px;overflow:hidden;border-radius:13px;background:linear-gradient(135deg,#ff3d81,#7657ff)}.candidate-art{width:100%;height:100%}.candidate strong{display:block;font-size:13px;margin-top:5px;line-height:1.2}.candidate-type{display:inline-flex;padding:4px 7px;border-radius:99px;background:#eeeafc;color:#5744a8;font-size:9px;font-weight:850;text-transform:uppercase}.candidate-meta{display:block;color:#717282;font-size:10px;line-height:1.4;margin-top:4px}.no-results{padding:28px;text-align:center;border:1px dashed #d5d5e0;border-radius:18px;color:#717282}.profile{border-radius:25px;padding:23px;color:#fff;background:linear-gradient(135deg,#11111b,#30205b 60%,#d82f71);box-shadow:0 18px 45px #34205d28}.profile-top{display:grid;grid-template-columns:112px 1fr auto;gap:19px;align-items:center}.profile-art{width:112px;height:112px;border-radius:22px;object-fit:cover;background:#ffffff16}.profile-name{font-size:32px;font-weight:900;letter-spacing:-.045em}.profile-kind{display:inline-flex;margin-bottom:7px;padding:5px 9px;border-radius:99px;background:#ffffff18;border:1px solid #ffffff25;font-size:10px;font-weight:850;text-transform:uppercase}.profile-summary{margin-top:8px;color:#ddd9e8;font-size:13px;line-height:1.55}.profile-actions{font-size:12px;font-weight:850;color:#fff;background:#ffffff18;border:1px solid #ffffff2b;border-radius:999px;padding:9px 13px;cursor:pointer}.profile-stats{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin-top:18px}.profile-stat{padding:12px;border-radius:15px;background:#ffffff12;border:1px solid #ffffff18}.profile-stat strong{display:block;font-size:21px}.profile-stat span{font-size:10px;color:#d7d3e2}.chart-summary{border-radius:23px;padding:20px;background:#fff;border:1px solid #e4e4ed;box-shadow:0 8px 28px #24243b0d}.chart-summary h3{margin:0;font-size:18px}.charts{display:grid;gap:8px;margin-top:13px}.chart{display:grid;grid-template-columns:1fr 42px 88px 180px;gap:9px;align-items:center;padding:12px;border-radius:13px;background:#f8f8fc}.source-name{font-size:11px;font-weight:700}.rankno{font-weight:900}.move{font-size:11px;font-weight:800;text-align:center;padding:5px;border-radius:99px;background:#eeeef4}.chart-meta{font-size:10px;color:#717282;text-align:right;line-height:1.4}.song-group{border:1px solid #e4e4ed;border-radius:18px;background:#fff;overflow:hidden;margin-top:10px}.song-head{display:flex;justify-content:space-between;gap:12px;align-items:center;padding:14px 16px;background:#fafafd;border-bottom:1px solid #eeeef3}.song-head strong{font-size:14px}.song-head span{font-size:10px;color:#717282}.song-group .charts{margin:0;padding:10px}.history-list{display:grid;gap:8px;margin-top:13px}.history-item{display:grid;grid-template-columns:100px 1fr auto;gap:10px;align-items:center;padding:10px 12px;border-radius:12px;background:#f8f8fc;font-size:11px}.history-item span{color:#717282}.chart-status{margin-top:13px;padding:14px 16px;border-radius:16px;background:#f1efff;border:1px solid #ddd7ff;color:#4c4478;font-size:12px;line-height:1.5}@media(max-width:900px){.result-sections{grid-template-columns:1fr 1fr}}@media(max-width:700px){.result-sections{grid-template-columns:1fr}.result-section.other{grid-column:auto}.search-results-head{display:block}.profile{padding:16px}.profile-top{grid-template-columns:64px 1fr}.profile-art{width:64px;height:64px;border-radius:14px}.profile-name{font-size:23px}.profile-actions{grid-column:1/-1;width:max-content}.profile-stats{grid-template-columns:1fr 1fr}.chart{grid-template-columns:1fr 38px 72px}.chart-meta{grid-column:1/-1;text-align:left;padding-top:2px;border-top:1px solid #e9e9f0}.song-head{align-items:flex-start;flex-direction:column}.history-item{grid-template-columns:1fr auto}.history-item span{grid-column:1/-1}}
html[data-theme="dark"] .search-results-panel,html[data-theme="dark"] .chart-summary{background:#1a1a21;border-color:#34343d}
`;d.head.appendChild(s)}
function status(t){let x=$('#searchStatus');if(!x){x=d.createElement('div');x.id='searchStatus';x.className='search-status';$('#updated')?.after(x)}x.textContent=t}
function snapshotEntries(snapshot,date=''){return snapshot?.sources?.flatMap(s=>(s.entries||[]).map(e=>({...e,source:s.name,sourceId:s.id,status:s.status,snapshotDate:date||snapshot.generatedAt?.slice(0,10)||''}))).filter(e=>e.status==='ok'&&Number.isFinite(Number(e.rank)))||[]}
function build(){const m=new Map();for(const e of all){const aa=artists(e),artist=aa.join(', ');for(const a of aa){const k='artist|'+norm(a),x=m.get(k);if(!x)m.set(k,{kind:'Artist',name:a,artist:a,artworkUrl:e.artworkUrl||''});else if(!x.artworkUrl&&e.artworkUrl)x.artworkUrl=e.artworkUrl}if(e.title){const k='song|'+norm(e.title)+'|'+norm(artist),x=m.get(k);if(!x)m.set(k,{kind:'Song',name:e.title,artist,album:album(e),artworkUrl:e.artworkUrl||''});else if(!x.artworkUrl&&e.artworkUrl)x.artworkUrl=e.artworkUrl}if(album(e)){const k='album|'+norm(album(e))+'|'+norm(artist),x=m.get(k);if(!x)m.set(k,{kind:'Album',name:album(e),artist,artworkUrl:e.artworkUrl||''});else if(!x.artworkUrl&&e.artworkUrl)x.artworkUrl=e.artworkUrl}}index=[...m.values()]}
function score(v,q){const n=norm(v),x=norm(q);if(!n||!x)return 0;if(n===x)return 1000;if(n.startsWith(x))return 900-Math.min(100,(n.length-x.length)*.7);if(n.includes(x))return 780-Math.min(120,(n.length-x.length)*.35);let h=0;for(const w of x.split(' ')){if(n.split(' ').includes(w))h+=80;else if(n.split(' ').some(v=>v.startsWith(w)))h+=50;else if(n.split(' ').some(v=>lev(v,w)<=Math.max(1,Math.floor(w.length/4))))h+=35}return h+Math.max(0,1-lev(n,x)/Math.max(n.length,x.length))*120}
function local(q){return index.map(x=>({...x,_score:Math.max(score(x.name,q),score(x.artist,q)*.92,score(x.album,q)*.78)})).filter(x=>x._score>=35).sort((a,b)=>b._score-a._score)}
function dedupe(xs){const m=new Map();for(const x of xs){const k=x.kind+'|'+norm(x.name)+'|'+norm(x.artist);if(!m.has(k)||x._score>m.get(k)._score)m.set(k,x)}return[...m.values()]}
function groups(xs){const g={Artist:[],Song:[],Album:[],Other:[]};for(const x of xs)g[g[x.kind]?x.kind:'Other'].push(x);Object.values(g).forEach(x=>x.sort((a,b)=>b._score-a._score));return g}
function exact(c){const n=norm(c.name),a=norm(c.artist||'');if(c.kind==='Artist')return all.filter(e=>artists(e).some(x=>norm(x)===n));if(c.kind==='Song')return all.filter(e=>norm(e.title)===n&&(!a||artists(e).some(x=>norm(x)===a)));return all.filter(e=>norm(album(e))===n&&(!a||artists(e).some(x=>norm(x)===a)))}
function card(c){return `<button class="candidate" type="button" data-kind="${esc(c.kind)}" data-name="${esc(c.name)}" data-artist="${esc(c.artist||'')}"><span class="candidate-art-wrap">${c.artworkUrl?`<img class="candidate-art" src="${esc(c.artworkUrl)}" alt="" loading="lazy">`:'<span class="candidate-art" aria-hidden="true"></span>'}</span><span><span class="candidate-type">${esc(c.kind)}</span><strong>${esc(c.name)}</strong><span class="candidate-meta">${esc(c.kind==='Artist'?'Artist':[c.artist,c.album].filter(Boolean).join(' · ')||'No additional metadata')}</span></span></button>`}
function renderResults(q){const g=groups(dedupe(local(q))),total=Object.values(g).reduce((n,x)=>n+x.length,0);$('#title').textContent='Search results';$('#sub').textContent='Matches are grouped by entity type and ranked within each section.';$('#count').textContent=total+' result'+(total===1?'':'s');if(!total){$('#results').innerHTML='<div class="empty">No matching artist, song or album was found in the tracked data.</div>';return}const labels=[['Artist','Artist'],['Song','Song / Track / Music / Record / Mixtape'],['Album','Album / EP'],['Other','Other']];$('#results').innerHTML=`<section class="search-results-panel"><div class="result-sections">${labels.map(([k,label])=>!g[k].length?'':`<section class="result-section ${k==='Other'?'other':''}"><div class="result-section-head"><strong>${label}</strong><span>${g[k].length}</span></div><div class="candidate-grid">${g[k].slice(0,12).map(card).join('')}</div></section>`).join('')}</div></section>`;bindCards();hydrate([...d.querySelectorAll('.candidate')].map(button=>({button,candidate:{kind:button.dataset.kind,name:button.dataset.name,artist:button.dataset.artist}})))}
function bindCards(){$('#results')?.querySelectorAll('.candidate').forEach(b=>b.addEventListener('click',()=>{const c={kind:b.dataset.kind,name:b.dataset.name,artist:b.dataset.artist};select(index.find(x=>x.kind===c.kind&&norm(x.name)===norm(c.name)&&norm(x.artist||'')===norm(c.artist||''))||c)}))}
function suggestions(q){const box=$('#searchSuggestions');if(!box)return;const g=groups(dedupe(local(q))),list=[...g.Artist,...g.Song,...g.Album,...g.Other].slice(0,8);if(!q||!list.length){box.classList.remove('open');return}box.innerHTML=list.map((c,i)=>`<button class="suggestion" type="button" data-i="${i}">${c.artworkUrl?`<img class="suggestion-art" src="${esc(c.artworkUrl)}" alt="">`:'<span class="suggestion-art"></span>'}<span class="suggestion-main"><strong>${esc(c.name)}</strong><span>${esc([c.kind,c.artist&&c.kind!=='Artist'?c.artist:'',c.album&&c.kind==='Song'?c.album:''].filter(Boolean).join(' · '))}</span></span><span class="suggestion-type">${esc(c.kind)}</span></button>`).join('');box.classList.add('open');box.querySelectorAll('[data-i]').forEach(b=>b.addEventListener('click',()=>select(list[+b.dataset.i])))}
async function appleArtwork(c){const key=`${c.kind}|${norm(c.name)}|${norm(c.artist||'')}`;if(artworkCache.has(key))return artworkCache.get(key);if(c.artworkUrl){artworkCache.set(key,c.artworkUrl);return c.artworkUrl}const term=encodeURIComponent([c.name,c.artist].filter(Boolean).join(' ')),entity=c.kind==='Album'?'album':'song';try{const r=await fetch(`https://itunes.apple.com/search?term=${term}&entity=${entity}&limit=8&country=AU`);if(!r.ok)throw Error();const j=await r.json(),wanted=norm(c.name),artist=norm(c.artist||''),m=(j.results||[]).find(x=>norm(c.kind==='Album'?x.collectionName:x.trackName)===wanted&&(!artist||norm(x.artistName)===artist))||j.results?.[0],url=m?.artworkUrl100?.replace(/100x100/g,'1200x1200')||'';artworkCache.set(key,url);return url}catch{artworkCache.set(key,'');return ''}}
async function hydrate(items){await Promise.all(items.map(async({button,candidate})=>{const e=index.find(x=>x.kind===candidate.kind&&norm(x.name)===norm(candidate.name)&&norm(x.artist||'')===norm(candidate.artist||''))||candidate;if(e.artworkUrl)return;const url=await appleArtwork(e);if(!url||!button.isConnected)return;const w=button.querySelector('.candidate-art-wrap');if(w)w.innerHTML=`<img class="candidate-art" src="${esc(url)}" alt="" loading="lazy">`})))}
function row(e){const mv=Number.isFinite(+e.movement)?+e.movement:Number.isFinite(+e.previousRank)?+e.previousRank-+e.rank:null,mt=mv===null?'NEW':mv>0?'↑ '+mv:mv<0?'↓ '+Math.abs(mv):'—',meta=[Number.isFinite(+e.previousRank)?'Previous #'+e.previousRank:'',Number.isFinite(+e.peakRank)?'Peak #'+e.peakRank:'',Number.isFinite(+e.weeksOnChart)?e.weeksOnChart+' weeks':'',text(e,'chartDate','date','period','weekEnding')].filter(Boolean).join(' · ');return `<div class="chart"><div class="source-name">${esc(e.source)}</div><div class="rankno">#${esc(e.rank)}</div><div class="move">${mt}</div><div class="chart-meta">${esc(meta||'Chart metadata not supplied')}</div></div>`}
function historyFor(c){const m=new Map();exact(c).forEach(e=>{if(!e.snapshotDate)return;if(!m.has(e.snapshotDate))m.set(e.snapshotDate,[]);m.get(e.snapshotDate).push(e)});return[...m.entries()].sort((a,b)=>b[0].localeCompare(a[0]))}
function summary(c,records){const sources=new Set(records.map(e=>e.source));return `${c.name}${c.artist?' by '+c.artist:''} has ${records.length} tracked chart placement${records.length===1?'':'s'} across ${sources.size} source${sources.size===1?'':'s'} in the available snapshots.`}
async function select(c){selected=c;$('#searchSuggestions')?.classList.remove('open');const records=exact(c),date=current?.generatedAt?.slice(0,10)||'',now=records.filter(e=>e.snapshotDate===date),hist=historyFor(c),sources=new Set(now.map(e=>e.source)),best=now.length?Math.min(...now.map(e=>+e.rank)):'—';let art=c.artworkUrl||now.find(e=>e.artworkUrl)?.artworkUrl||'';if(!art)art=await appleArtwork(c);$('#title').textContent=c.kind+' profile';$('#sub').textContent='Selected entity with current and historical chart performance.';$('#count').textContent=now.length+' current placement'+(now.length===1?'':'s');let body=`<section class="profile"><div class="profile-top">${art?`<img class="profile-art" src="${esc(art)}" alt="">`:'<div class="profile-art" aria-hidden="true"></div>'}<div><span class="profile-kind">${esc(c.kind)}</span><div class="profile-name">${esc(c.name)}</div><div class="profile-summary">${esc(summary(c,records))}</div></div><button class="profile-actions" id="backToResults" type="button">Change result</button></div><div class="profile-stats"><div class="profile-stat"><strong>${best==='—'?'—':'#'+best}</strong><span>best current position</span></div><div class="profile-stat"><strong>${sources.size}</strong><span>current chart sources</span></div><div class="profile-stat"><strong>${records.length}</strong><span>all tracked placements</span></div><div class="profile-stat"><strong>${hist.length}</strong><span>dated snapshots</span></div><div class="profile-stat"><strong>${esc(c.artist||'—')}</strong><span>artist</span></div></div></section>`;if(c.kind==='Artist'){const gs=new Map();now.forEach(e=>{const k=norm(e.title);if(!gs.has(k))gs.set(k,{title:e.title,rows:[]});gs.get(k).rows.push(e)});body+=`<section class="chart-summary"><h3>Current chart performance by song</h3>${gs.size?[...gs.values()].sort((a,b)=>Math.min(...a.rows.map(e=>e.rank))-Math.min(...b.rows.map(e=>e.rank))).map(g=>`<section class="song-group"><div class="song-head"><strong>${esc(g.title)}</strong><span>${g.rows.length} chart placement${g.rows.length===1?'':'s'}</span></div><div class="charts">${g.rows.sort((a,b)=>a.rank-b.rank).map(row).join('')}</div></section>`).join(''):'<div class="no-results">No current chart placement is in the tracked data.</div>'}</section>`}else body+=`<section class="chart-summary"><h3>Current chart performance</h3><div class="charts">${now.length?now.sort((a,b)=>a.rank-b.rank).map(row).join(''):'<div class="no-results">No current chart placement is in the tracked data.</div>'}</div></section>`;body+=`<section class="chart-summary"><h3>History</h3>${hist.length?`<div class="history-list">${hist.map(([date,rows])=>`<div class="history-item"><strong>${esc(date)}</strong><span>${rows.length} placement${rows.length===1?'':'s'} · ${esc([...new Set(rows.map(e=>e.source))].join(', '))}</span><b>Best #${Math.min(...rows.map(e=>+e.rank))}</b></div>`).join('')}</div>`:'<div class="no-results">No historical snapshot matches this entity yet.</div>'}</section><section class="chart-status">Source attribution: chart placements remain under their original tracked source. Artwork fallback uses Apple’s public catalogue search only when StrettoCharts does not already have trusted artwork.</section>`;$('#results').innerHTML=body;$('#backToResults')?.addEventListener('click',()=>renderResults($('#search').value.trim()))}
async function load(){const id=++request;status('Loading chart data…');try{const r=await fetch('data/latest.json',{cache:'no-store'});if(!r.ok)throw Error('latest snapshot unavailable');current=await r.json();const ir=await fetch('data/history/index.json',{cache:'no-store'}),dates=ir.ok?await ir.json():[],currentDate=current.generatedAt?.slice(0,10)||'',historicalDates=dates.filter(date=>date!==currentDate),snapshots=await Promise.all(historicalDates.map(async date=>{try{const x=await fetch(`data/history/${encodeURIComponent(date)}.json`,{cache:'no-store'});return x.ok?await x.json():null}catch{return null}}));if(id!==request)return;all=snapshotEntries(current,currentDate).concat(...snapshots.filter(Boolean).map((s,i)=>snapshotEntries(s,historicalDates[i])));build();status(`${index.length} searchable entities loaded across current and historical snapshots.`);const u=$('#updated');if(u&&current.generatedAt)u.textContent=`Chart data updated ${new Date(current.generatedAt).toLocaleString()}.`}catch(e){status('Search data could not be loaded. Refresh and try again.');console.error(e)}}
function runSearch(q){q=String(q||'').trim();$('#searchSuggestions')?.classList.remove('open');if(!q){$('#title').textContent='Search StrettoCharts';$('#sub').textContent='Your results will appear here.';$('#count').textContent='';$('#results').innerHTML='<div class="empty">Search for an artist, song or album to see its chart performance.</div>';return}if(!index.length){status('Search data is still loading…');return}renderResults(q)}
function init(){style();const form=$('#searchForm'),input=$('#search');if(!form||!input)return;if(!$('#searchSuggestions')){const box=d.createElement('div');box.id='searchSuggestions';box.className='search-suggestions';form.style.position='relative';form.appendChild(box)}form.addEventListener('submit',e=>{e.preventDefault();runSearch(input.value)});input.addEventListener('input',()=>{clearTimeout(timer);const q=input.value.trim();timer=setTimeout(()=>suggestions(q),90)});input.addEventListener('keydown',e=>{if(e.key==='Escape')$('#searchSuggestions')?.classList.remove('open')});d.addEventListener('click',e=>{if(!e.target.closest('.searchbar'))$('#searchSuggestions')?.classList.remove('open')});load()}
if(d.readyState==='loading')d.addEventListener('DOMContentLoaded',init);else init();
})();

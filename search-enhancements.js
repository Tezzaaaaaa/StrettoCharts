/* StrettoCharts search: fast local index, typo-tolerant ranking, autocomplete, disambiguation, and complete chart results. */
(function(){
  const d=document,$=s=>d.querySelector(s);
  const norm=s=>String(s??'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/&/g,' and ').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();
  const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
  const words=s=>norm(s).split(' ').filter(Boolean);
  const text=(e,...ks)=>{for(const k of ks)if(e?.[k]!=null&&String(e[k]).trim())return String(e[k]).trim();return ''};
  const album=e=>text(e,'album','albumName','release','releaseTitle');
  const artists=e=>Array.isArray(e?.artists)?e.artists:[];
  const key=(kind,name,artist='')=>kind+'::'+norm(name)+'::'+norm(artist);
  let data=null,all=[],index=[],candidates=[],active=-1,searchTimer=null,request=0;
  const cache=new Map();

  function addStyle(){
    if($('#stretto-search-css'))return;
    const s=d.createElement('style');s.id='stretto-search-css';s.textContent=`
      .searchbar{position:relative}.searchbox{position:relative;min-width:0}.search{padding-right:52px}.search-clear{position:absolute;right:12px;top:12px;width:40px;height:40px;border:0;border-radius:50%;background:transparent;color:#aaa7b7;font-size:20px;cursor:pointer;display:none}.search-clear:hover{background:#ffffff16;color:#fff}.search-suggestions{position:absolute;left:0;right:0;top:72px;z-index:30;background:#fff;color:#171722;border:1px solid #e4e4ed;border-radius:18px;padding:7px;box-shadow:0 18px 45px #0004;display:none;max-height:390px;overflow:auto}.search-suggestions.open{display:block}.suggestion{width:100%;display:grid;grid-template-columns:42px 1fr auto;gap:10px;align-items:center;text-align:left;border:0;background:transparent;border-radius:12px;padding:9px;cursor:pointer}.suggestion:hover,.suggestion.active{background:#f3f1fb}.suggestion-art{width:42px;height:42px;border-radius:10px;object-fit:cover;background:linear-gradient(135deg,#ff3d81,#7657ff)}.suggestion-main{min-width:0}.suggestion-main strong,.suggestion-main span{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.suggestion-main strong{font-size:13px}.suggestion-main span{font-size:10px;color:#717282;margin-top:3px}.suggestion-type{font-size:9px;font-weight:850;text-transform:uppercase;letter-spacing:.08em;color:#6252aa}.search-help{margin-top:9px;color:#aaa7b7;font-size:11px}.search-help kbd{border:1px solid #ffffff25;border-bottom-color:#ffffff35;border-radius:5px;padding:1px 5px;font-size:9px}.search-status{margin-top:10px;color:#c9c5d7;font-size:12px;min-height:18px}.search-results-panel{background:#fff;border:1px solid #e4e4ed;border-radius:23px;padding:20px;box-shadow:0 8px 28px #24243b0d}.search-results-head{display:flex;justify-content:space-between;gap:15px;align-items:center;margin-bottom:15px}.search-results-head strong{display:block;font-size:18px}.search-results-head span{display:block;color:#717282;font-size:12px;margin-top:4px}.search-results-head button{border:0;border-radius:999px;padding:9px 13px;background:#11111b;color:#fff;font-size:11px;font-weight:800;cursor:pointer}.candidate-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.candidate{display:grid;grid-template-columns:58px 1fr;gap:12px;text-align:left;align-items:center;border:1px solid #e4e4ed;background:#fafafd;border-radius:17px;padding:10px;cursor:pointer}.candidate:hover,.candidate:focus-visible{border-color:#7657ff;transform:translateY(-1px);outline:0}.candidate img,.candidate-art{width:58px;height:58px;border-radius:13px;object-fit:cover;background:linear-gradient(135deg,#ff3d81,#7657ff)}.candidate-type{display:inline-flex;padding:4px 7px;border-radius:99px;background:#eeeafc;color:#5744a8;font-size:9px;font-weight:850;text-transform:uppercase;letter-spacing:.08em}.candidate strong{display:block;font-size:14px;margin-top:6px}.candidate-meta{display:block;color:#717282;font-size:10px;line-height:1.4;margin-top:4px}.no-results{padding:28px;text-align:center;border:1px dashed #d5d5e0;border-radius:18px;color:#717282}.no-results strong{display:block;color:#171722;font-size:16px;margin-bottom:5px}.profile{border-radius:25px;padding:23px;color:#fff;background:linear-gradient(135deg,#11111b,#30205b 60%,#d82f71);box-shadow:0 18px 45px #34205d28}.profile-top{display:grid;grid-template-columns:112px 1fr auto;gap:19px;align-items:center}.profile-art{width:112px;height:112px;border-radius:22px;object-fit:cover;background:#ffffff16}.profile-name{font-size:32px;font-weight:900;letter-spacing:-.045em}.profile-kind{display:inline-flex;margin-bottom:7px;padding:5px 9px;border-radius:99px;background:#ffffff18;border:1px solid #ffffff25;font-size:10px;font-weight:850;text-transform:uppercase;letter-spacing:.1em}.profile-summary{margin-top:8px;color:#ddd9e8;font-size:13px;line-height:1.55;max-width:850px}.profile-actions{font-size:12px;font-weight:850;color:#fff;background:#ffffff18;border:1px solid #ffffff2b;border-radius:999px;padding:9px 13px;white-space:nowrap;cursor:pointer}.profile-stats{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin-top:18px}.profile-stat{padding:12px;border-radius:15px;background:#ffffff12;border:1px solid #ffffff18}.profile-stat strong{display:block;font-size:21px}.profile-stat span{font-size:10px;color:#d7d3e2}.chart-summary{border-radius:23px;padding:20px;background:#fff;border:1px solid #e4e4ed;box-shadow:0 8px 28px #24243b0d}.chart-summary h3{margin:0;font-size:18px}.summary-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-top:13px}.summary-item{padding:13px;border-radius:16px;background:#f8f8fc}.summary-item strong{display:block;font-size:22px}.summary-item span{font-size:10px;color:#717282}.charts{display:grid;gap:8px;margin-top:13px}.chart{display:grid;grid-template-columns:1fr 42px 88px 180px;gap:9px;align-items:center;padding:12px;border-radius:13px;background:#f8f8fc}.source-name{font-size:11px;font-weight:700}.rankno{font-weight:900}.move{font-size:11px;font-weight:800;text-align:center;padding:5px;border-radius:99px;background:#eeeef4}.chart-meta{font-size:10px;color:#717282;text-align:right;line-height:1.4}.song-group{border:1px solid #e4e4ed;border-radius:18px;background:#fff;overflow:hidden;margin-top:10px}.song-head{display:flex;justify-content:space-between;gap:12px;align-items:center;padding:14px 16px;background:#fafafd;border-bottom:1px solid #eeeef3}.song-head strong{font-size:14px}.song-head span{font-size:10px;color:#717282}.song-group .charts{margin:0;padding:10px}.album-info{border-radius:23px;padding:20px;background:#fff;border:1px solid #e4e4ed;box-shadow:0 8px 28px #24243b0d}.album-info h3{margin:0;font-size:18px}.album-meta{display:flex;flex-wrap:wrap;gap:7px;margin:12px 0}.album-meta span{padding:6px 9px;border-radius:99px;background:#f1efff;color:#4c4478;font-size:10px}.track-list{display:grid;gap:7px;margin-top:13px}.track-item{display:grid;grid-template-columns:34px 1fr auto;gap:10px;align-items:center;padding:9px 10px;border-radius:12px;background:#f8f8fc;font-size:11px}.track-item b,.track-item small{color:#717282}.chart-status{margin-top:13px;padding:14px 16px;border-radius:16px;background:#f1efff;border:1px solid #ddd7ff;color:#4c4478;font-size:12px;line-height:1.5}@media(max-width:900px){.candidate-grid{grid-template-columns:1fr 1fr}}@media(max-width:700px){.search-suggestions{top:72px}.candidate-grid{grid-template-columns:1fr}.search-results-head{display:block}.search-results-head button{margin-top:10px}.profile{padding:16px}.profile-top{grid-template-columns:64px 1fr}.profile-art{width:64px;height:64px;border-radius:14px}.profile-name{font-size:23px}.profile-actions{grid-column:1/-1;width:max-content}.profile-stats{grid-template-columns:1fr 1fr}.summary-grid{grid-template-columns:1fr 1fr}.chart{grid-template-columns:1fr 38px 72px}.chart-meta{grid-column:1/-1;text-align:left;padding-top:2px;border-top:1px solid #e9e9f0}.song-head{align-items:flex-start;flex-direction:column}}@media(prefers-reduced-motion:reduce){.candidate:hover{transform:none}}
    `;d.head.appendChild(s);
  }

  function lev(a,b){if(a===b)return 0;if(!a)return b.length;if(!b)return a.length;let p=Array(b.length+1).fill(0),q=Array(b.length+1);for(let j=0;j<=b.length;j++)p[j]=j;for(let i=1;i<=a.length;i++){q[0]=i;for(let j=1;j<=b.length;j++)q[j]=Math.min(q[j-1]+1,p[j]+1,p[j-1]+(a[i-1]===b[j-1]?0:1));[p,q]=[q,p]}return p[b.length]}
  function score(name,q,kind){const n=norm(name),qq=norm(q);if(!n||!qq)return 0;if(n===qq)return 1000;if(n.startsWith(qq))return 900-((n.length-qq.length)*.7);if(n.includes(qq))return 780-((n.length-qq.length)*.35);const nw=words(n),qw=words(qq);let hits=0;for(const w of qw){if(nw.some(x=>x===w)){hits+=80;continue}if(nw.some(x=>x.startsWith(w))){hits+=50;continue}const best=Math.min(...nw.map(x=>lev(x,w)));if(best<=Math.max(1,Math.floor(w.length/4)))hits+=35}const d=lev(n,qq),sim=Math.max(0,1-d/Math.max(n.length,qq.length));return hits+(sim*120)+(kind==='Artist'&&qw.length===1?8:0)}
  function localCandidates(q){const scored=[];for(const x of index){const fields=[x.name,x.artist||'',x.album||''];const s=Math.max(score(x.name,q,x.kind),score(x.artist,q,'Artist')*.92,score(x.album,q,'Album')*.78);if(s>=35)scored.push({...x,_score:s})}return unique(scored).sort((a,b)=>b._score-a._score||a.kind.localeCompare(b.kind)).slice(0,18)}
  function unique(xs){const m=new Map();for(const x of xs){if(!x?.name)continue;const k=key(x.kind,x.name,x.artist||'');if(!m.has(k)||x._score>(m.get(k)._score||0))m.set(k,x)}return [...m.values()]}
  function entries(){return data?.sources?.flatMap(s=>(s.entries||[]).map(e=>({...e,source:s.name,status:s.status}))).filter(e=>e.status==='ok'&&Number.isFinite(Number(e.rank)))||[]}
  function buildIndex(){all=entries();const m=new Map();for(const e of all){for(const a of artists(e)){const k=key('Artist',a);if(!m.has(k))m.set(k,{kind:'Artist',name:a,artist:a,artworkUrl:e.artworkUrl})}const a=artists(e).join(', ');if(e.title){const k=key('Song',e.title,a);if(!m.has(k))m.set(k,{kind:'Song',name:e.title,artist:a,album:album(e),artworkUrl:e.artworkUrl})}if(album(e)){const k=key('Album',album(e),a);if(!m.has(k))m.set(k,{kind:'Album',name:album(e),artist:a,artworkUrl:e.artworkUrl})}}index=[...m.values()]}
  async function iTunes(q,entity){const ck=entity+'::'+norm(q);if(cache.has(ck))return cache.get(ck);try{const u='https://itunes.apple.com/search?term='+encodeURIComponent(q)+'&media=music&entity='+entity+'&limit=20&country=AU';const r=await fetch(u,{signal:AbortSignal.timeout(4500)});if(!r.ok)return[];const x=await r.json();const out=(x.results||[]).map(v=>entity==='musicArtist'?{kind:'Artist',name:v.artistName,artist:v.artistName,genre:v.primaryGenreName,artworkUrl:v.artworkUrl100,url:v.artistLinkUrl}:entity==='album'?{kind:'Album',name:v.collectionName,artist:v.artistName,releaseDate:v.releaseDate,trackCount:v.trackCount,genre:v.primaryGenreName,artworkUrl:v.artworkUrl100,url:v.collectionViewUrl,id:v.collectionId}: {kind:'Song',name:v.trackName,artist:v.artistName,album:v.collectionName,releaseDate:v.releaseDate,artworkUrl:v.artworkUrl100,url:v.trackViewUrl,id:v.trackId});cache.set(ck,out);return out}catch(_){return[]}}
  function showStatus(t){const x=$('#searchStatus');if(x)x.textContent=t}
  function candidateCard(c,i){const es=exactEntries(c),meta=[c.artist,c.album,c.releaseDate?new Date(c.releaseDate).getFullYear():'',c.trackCount?c.trackCount+' tracks':'',es.length?es.length+' current placement'+(es.length===1?'':'s'):''].filter(Boolean).join(' · ');return `<button class="candidate" type="button" data-result="${i}" aria-label="${esc(c.kind+': '+c.name)}">${c.artworkUrl?`<img src="${esc(c.artworkUrl)}" alt="" loading="lazy">`:'<div class="candidate-art"></div>'}<div><span class="candidate-type">${esc(c.kind)}</span><strong>${esc(c.name)}</strong><span class="candidate-meta">${esc(meta||'No current StrettoCharts placement')}</span></div></button>`}
  function renderSuggestions(list,q){const box=$('#searchSuggestions');if(!box)return;active=-1;if(!q||!list.length){box.innerHTML='';box.classList.remove('open');return}box.innerHTML=list.slice(0,8).map((c,i)=>`<button class="suggestion" type="button" data-suggest="${i}">${c.artworkUrl?`<img class="suggestion-art" src="${esc(c.artworkUrl)}" alt="" loading="lazy">`:'<div class="suggestion-art"></div>'}<div class="suggestion-main"><strong>${esc(c.name)}</strong><span>${esc([c.kind,c.artist&&c.kind!=='Artist'?c.artist:'',c.album&&c.kind==='Song'?c.album:''].filter(Boolean).join(' · '))}</span></div><span class="suggestion-type">${esc(c.kind)}</span></button>`).join('');box.classList.add('open');box.querySelectorAll('[data-suggest]').forEach(b=>b.onclick=()=>chooseSuggestion(list[+b.dataset.suggest)]);}
  function showCandidates(q){candidates.sort((a,b)=>(b._score||0)-(a._score||0));$('#title').textContent='Search results for “'+q+'”';$('#sub').textContent=candidates.length?'Choose the exact artist, song or album you mean.':'No strong match found — try a title, artist, or a shorter spelling.';$('#count').textContent=candidates.length?candidates.length+' result'+(candidates.length===1?'':'s'):'';$('#results').innerHTML=candidates.length?`<div class="search-results-panel"><div class="search-results-head"><div><strong>Choose your result</strong><span>Results are ranked by exact match, relevance, artist and song context.</span></div><button id="changeSearch" type="button">Change search</button></div><div class="candidate-grid">${candidates.map(candidateCard).join('')}</div></div>`:`<div class="no-results"><strong>Nothing close enough to “${esc(q)}”.</strong>Try the full artist name, song title, or album name.</div>`;d.querySelectorAll('[data-result]').forEach(b=>b.onclick=()=>select(candidates[+b.dataset.result]));$('#changeSearch')?.addEventListener('click',()=>{$('#search').focus();$('#search').select()});showStatus(candidates.length?'Search complete — select the result you mean.':'No confident match.');}
  function exactEntries(c){const n=norm(c.name),a=norm(c.artist||'');if(c.kind==='Artist')return all.filter(e=>artists(e).some(x=>norm(x)===n));if(c.kind==='Song')return all.filter(e=>norm(e.title)===n&&(!a||artists(e).some(x=>norm(x)===a)));return all.filter(e=>norm(album(e))===n&&(!a||artists(e).some(x=>norm(x)===a)))}
  function chartRow(e){const mv=Number.isFinite(+e.movement)?+e.movement:Number.isFinite(+e.previousRank)?+e.previousRank-+e.rank:null;const mt=mv===null?'NEW':mv>0?'↑ '+mv:mv<0?'↓ '+Math.abs(mv):'—';return `<div class="chart"><div><div class="source-name">${esc(e.source)}</div></div><div class="rankno">#${e.rank}</div><div class="move">${mt}</div><div class="chart-meta">${esc([Number.isFinite(+e.previousRank)?'Previous #'+e.previousRank:'',Number.isFinite(+e.peakRank)?'Peak #'+e.peakRank:'',Number.isFinite(+e.weeksOnChart)?e.weeksOnChart+' weeks':'',text(e,'chartDate','date','period','weekEnding')].filter(Boolean).join(' · ')||'Chart metadata not supplied')}</div></div>`}
  function renderArtistCharts(es){const groups=new Map();es.forEach(e=>{const k=norm(e.title);if(!groups.has(k))groups.set(k,{title:e.title,rows:[]});groups.get(k).rows.push(e)});return [...groups.values()].sort((a,b)=>Math.min(...a.rows.map(e=>e.rank))-Math.min(...b.rows.map(e=>e.rank))).map(g=>`<section class="song-group"><div class="song-head"><strong>${esc(g.title)}</strong><span>${g.rows.length} chart placement${g.rows.length===1?'':'s'}</span></div><div class="charts">${g.rows.sort((a,b)=>a.rank-b.rank).map(chartRow).join('')}</div></section>`).join('')}
  async function albumTracks(c){if(!c.id)return[];try{const r=await fetch('https://itunes.apple.com/lookup?id='+encodeURIComponent(c.id)+'&entity=song&country=AU',{signal:AbortSignal.timeout(4500)});if(!r.ok)return[];const x=await r.json();return(x.results||[]).filter(v=>v.wrapperType==='track').map(v=>({title:v.trackName,artist:v.artistName,album:v.collectionName,artworkUrl:v.artworkUrl100}))}catch(_){return[]}}
  async function select(c){active=-1;$('#searchSuggestions')?.classList.remove('open');showStatus('Loading '+c.kind.toLowerCase()+' profile…');const es=exactEntries(c);let tracks=[];if(c.kind==='Album'&&c.id)tracks=await albumTracks(c);const sources=new Set(es.map(e=>e.source)),best=es.length?Math.min(...es.map(e=>+e.rank)):'—',songs=c.kind==='Artist'?[...new Set(es.map(e=>e.title))]:c.kind==='Album'?[...new Set(tracks.map(t=>t.title))]:[c.name],art=c.artworkUrl||es.find(e=>e.artworkUrl)?.artworkUrl||'';$('#title').textContent=c.kind+' profile';$('#sub').textContent='Every matching current chart placement from successful StrettoCharts sources is shown below.';$('#count').textContent=es.length+' placement'+(es.length===1?'':'s');let body=`<section class="profile"><div class="profile-top"><img class="profile-art" src="${esc(art)}" alt="" onerror="this.style.visibility='hidden'"><div><span class="profile-kind">${esc(c.kind)} profile</span><div class="profile-name">${esc(c.name)}</div><div class="profile-summary">${esc(c.kind==='Album'?`${c.name} by ${c.artist||'Unknown artist'}. Album information is shown even when the album has no current chart placement.`:`${c.kind==='Artist'?c.name:c.name+(c.artist?' by '+c.artist:'')} — chart performance is kept source-specific, with no made-up combined rank.`)}</div></div><button class="profile-actions" id="backToResults" type="button">Change result</button></div><div class="profile-stats"><div class="profile-stat"><strong>${best==='—'?'—':'#'+best}</strong><span>best current position</span></div><div class="profile-stat"><strong>${sources.size}</strong><span>chart sources</span></div><div class="profile-stat"><strong>${es.length}</strong><span>chart placements</span></div><div class="profile-stat"><strong>${songs.length}</strong><span>${c.kind==='Artist'?'songs found':'tracks / song'}</span></div><div class="profile-stat"><strong>${c.trackCount||'—'}</strong><span>album tracks</span></div></div></section>`;
    if(c.kind==='Artist')body+=`<section class="chart-summary"><h3>Current chart performance by song</h3>${es.length?renderArtistCharts(es):'<div class="no-results">No current chart placement is in the tracked data.</div>'}</section>`;
    else if(c.kind==='Song')body+=`<section class="chart-summary"><h3>Current chart placements</h3><div class="charts">${es.length?es.sort((a,b)=>a.rank-b.rank).map(chartRow).join(''):'<div class="no-results">This song has no current placement in the tracked data.</div>'}</div></section>`;
    else body+=`<section class="album-info"><h3>Album information</h3><div class="album-meta">${[c.artist&&'Artist: '+c.artist,c.releaseDate&&'Released: '+new Date(c.releaseDate).toLocaleDateString(),c.trackCount&&'Tracks: '+c.trackCount,c.genre&&'Genre: '+c.genre].filter(Boolean).map(x=>`<span>${esc(x)}</span>`).join('')}</div><div class="chart-status"><b>Chart status:</b> ${es.length?'Currently represented in '+sources.size+' tracked source'+(sources.size===1?'':'s')+'.':'Not currently charting in any tracked StrettoCharts source.'}</div>${tracks.length?`<div class="track-list">${tracks.map((t,i)=>`<div class="track-item"><b>${i+1}</b><span>${esc(t.title)}</span><small>${esc(t.artist||'')}</small></div>`).join('')}</div>`:''}${es.length?`<div class="charts">${es.sort((a,b)=>a.rank-b.rank).map(chartRow).join('')}</div>`:''}</section>`;
    $('#results').innerHTML=body;$('#backToResults').onclick=()=>showCandidates($('#search').value.trim());showStatus('Profile loaded.');}
  async function runSearch(q){q=q.trim();if(!q)return;const id=++request;$('#search').value=q;$('#searchSuggestions')?.classList.remove('open');$('#search').blur();$('#confirm').prop?.('disabled',false);showStatus('Searching StrettoCharts and music metadata…');candidates=localCandidates(q);renderSuggestions(candidates,q);const [a,s,al]=await Promise.all([iTunes(q,'musicArtist'),iTunes(q,'song'),iTunes(q,'album')]);if(id!==request)return;candidates=unique([...candidates,...a,...s,...al].map(x=>({...x,_score:Math.max(x._score||0,score(x.name,q,x.kind)+(x.kind==='Artist'?18:0),score(x.artist||'',q,'Artist')*.86)))).filter(x=>(x._score||0)>=45).sort((x,y)=>(y._score||0)-(x._score||0)).slice(0,24);showCandidates(q)}
  function chooseSuggestion(c){$('#search').value=c.name;runSearch(c.name)}
  function moveSuggestion(dir){const box=$('#searchSuggestions');const items=[...(box?.querySelectorAll('.suggestion')||[])];if(!items.length)return;active=(active+dir+items.length)%items.length;items.forEach((x,i)=>x.classList.toggle('active',i===active));items[active]?.scrollIntoView({block:'nearest'});}
  async function init(){addStyle();const form=$('#searchForm'),input=$('#search');if(!form||!input)return;const box=d.createElement('div');box.id='searchSuggestions';box.className='search-suggestions';const wrap=input.parentElement;wrap?.classList.add('searchbox');wrap?.appendChild(box);const clear=d.createElement('button');clear.type='button';clear.className='search-clear';clear.setAttribute('aria-label','Clear search');clear.textContent='×';wrap?.appendChild(clear);input.addEventListener('input',()=>{clear.style.display=input.value?'block':'none';clearTimeout(searchTimer);const q=input.value.trim();searchTimer=setTimeout(()=>renderSuggestions(localCandidates(q),q),80)});input.addEventListener('keydown',e=>{if(e.key==='ArrowDown'){e.preventDefault();moveSuggestion(1)}else if(e.key==='ArrowUp'){e.preventDefault();moveSuggestion(-1)}else if(e.key==='Enter'&&$('#searchSuggestions')?.classList.contains('open')&&active>=0){e.preventDefault();const c=localCandidates(input.value.trim())[active];if(c)chooseSuggestion(c)}else if(e.key==='Escape')box.classList.remove('open')});input.addEventListener('focus',()=>{const q=input.value.trim();if(q)renderSuggestions(localCandidates(q),q)});clear.onclick=()=>{input.value='';clear.style.display='none';box.classList.remove('open');input.focus()};d.addEventListener('click',e=>{if(!wrap?.contains(e.target))box.classList.remove('open')});form.addEventListener('submit',e=>{e.preventDefault();runSearch(input.value)});showStatus('Loading chart index…');try{const r=await fetch('data/latest.json',{cache:'no-store'});if(!r.ok)throw new Error('data');data=await r.json();buildIndex();showStatus('Ready — search an artist, song or album.');}catch(_){showStatus('Chart index could not be loaded. Search may be unavailable until the data returns.')}}
  if(d.readyState==='loading')d.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();